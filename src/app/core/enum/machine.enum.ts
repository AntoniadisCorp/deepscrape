export enum MACHNINE_STATE {
    CREATED = 'created',
    STOPPED = 'stopped',
    STOPPING = 'stopping',
    STARTED = 'started',
    STARTING = 'starting',
    SUSPENDED = 'suspended',
    DESTROYED = 'destroyed'
}