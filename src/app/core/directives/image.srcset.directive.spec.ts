import { ImageSrcsetDirective } from './image.srcset.directive';

describe('ImageSrcsetDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSrcsetDirective();
    expect(directive).toBeTruthy();
  });
});
