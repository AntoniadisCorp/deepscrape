import { RemoveClassDirective } from './removeclass.directive';

describe('RemoveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveClassDirective();
    expect(directive).toBeTruthy();
  });
});
